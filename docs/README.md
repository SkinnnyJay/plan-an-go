# Documentation

| Doc | Contents |
|-----|----------|
| [COMMANDS.md](COMMANDS.md) | **Command reference:** argument tables, examples, and when to use each command (forever, run, validate, planner, prd, prd-from-plan, task-watcher, reset, plan-check). Covers plan compliance (`<work>`, `--strict`), output/workspace (`--out-dir`), plan override (`--plan`), and generating PLAN from PRD. |
| [ENV-README.md](ENV-README.md) | Environment variables (full table), output directory and cleanup (`--out-dir`, `--clean-after`, `--force`). |

See also: [README.md](../README.md) (Quick start, commands), [CLAUDE.md](../CLAUDE.md) (Commands, plan format), [.env.sample](../.env.sample) (copy to `.env`).
